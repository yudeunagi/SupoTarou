<Window x:Class="SupoTarou.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:SupoTarou.Controls"
        mc:Ignorable="d"
        Title="スポ太郎"
        ResizeMode="NoResize"
        Topmost="True"
        Height="420"
        Width="300"
        Background="#888888"
        >
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="1"
            BorderBrush="#33000000"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12"
            Margin="0,0,0,16">
            <Grid
                Width="240"
                Height="240"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <controls:HueRingControl
                    x:Name="HueRing"
                    RingThickness="24"
                    MarkerRadius="6" />
                <controls:SVBoxControl
                    x:Name="SvBox"
                    Margin="60"
                    MarkerRadius="6" />
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Margin="4,0,0,0">
            <TextBlock x:Name="HSVValueText" FontSize="16" FontFamily="Consolas" />
            <TextBlock x:Name="StateText" FontSize="14" FontWeight="SemiBold" Margin="0,8,0,0" />
        </StackPanel>

        <TextBlock
            Grid.Row="3"
            VerticalAlignment="Bottom"
            HorizontalAlignment="Center"
            Margin="0,5,0,0"
            Text="Spaceキー: 色の固定 / 固定解除"
            Foreground="#FF000000" />
    </Grid>
</Window>
